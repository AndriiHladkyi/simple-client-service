<p><b>Name: </b><%= @organization.name %></p>
<p><b>Tax id: </b><%= @organization.tax_id %></p>

<%= link_to 'Edit', edit_organization_path(@organization), class: 'btn btn-dark' %> 
<%= link_to 'Back', organizations_path, class: 'btn btn-dark' %>
